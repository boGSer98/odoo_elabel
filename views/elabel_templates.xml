<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <template id="wine_elabel_nutrition_table" name="Wine e-Label Nutrition Table">
        <table t-att-class="table_class or ''">
            <tbody>
                <tr t-foreach="product._get_elabel_nutrition_lines()" t-as="line">
                    <th t-att-style="th_style or ''">
                        <t t-esc="line['label']"/>
                    </th>
                    <td>
                        <t t-esc="line['value']"/>
                    </td>
                </tr>
            </tbody>
        </table>
    </template>

    <template id="wine_elabel_information" name="Wine e-Label Information">
        <t t-set="is_pdf" t-value="is_pdf or False"/>
        <t t-set="elabel_labels" t-value="product._get_elabel_display_labels()"/>

        <t t-if="is_pdf">
            <h3><t t-out="elabel_labels['nutrition_declaration']"/></h3>
            <p><strong><t t-out="elabel_labels['information_per_100_ml']"/></strong></p>
            <t t-set="table_class" t-value="'table table-sm table-bordered'"/>
            <t t-set="th_style" t-value="'width:55%;'"/>
            <t t-call="odoo_elabel.wine_elabel_nutrition_table"/>

            <p t-if="product.elabel_nutrition_note">
                <t t-esc="product.elabel_nutrition_note"/>
            </p>

            <h3><t t-out="elabel_labels['ingredients']"/></h3>
            <p>
                <t t-esc="product.elabel_ingredients or '-'"/>
            </p>

            <h3><t t-out="elabel_labels['allergens']"/></h3>
            <p>
                <t t-esc="product.elabel_allergen_statement or '-'"/>
            </p>

            <h3><t t-out="elabel_labels['additional_substances']"/></h3>
            <p>
                <t t-esc="product.elabel_additional_substances or '-'"/>
            </p>
        </t>

        <t t-else="">
            <h2><t t-out="elabel_labels['nutrition_declaration']"/></h2>
            <p class="pill"><t t-out="elabel_labels['information_per_100_ml']"/></p>
            <t t-set="table_class" t-value="''"/>
            <t t-set="th_style" t-value="''"/>
            <t t-call="odoo_elabel.wine_elabel_nutrition_table"/>

            <p class="muted" t-if="product.elabel_nutrition_note" style="margin-top:10px;">
                <t t-esc="product.elabel_nutrition_note"/>
            </p>

            <h2><t t-out="elabel_labels['ingredients']"/></h2>
            <div class="ingredient-box" t-if="product.elabel_ingredients">
                <t t-esc="product.elabel_ingredients"/>
            </div>
            <div class="ingredient-box" t-if="not product.elabel_ingredients">-</div>

            <h2><t t-out="elabel_labels['allergens']"/></h2>
            <div class="ingredient-box">
                <t t-esc="product.elabel_allergen_statement or '-'"/>
            </div>

            <h2><t t-out="elabel_labels['additional_substances']"/></h2>
            <div class="ingredient-box" t-if="product.elabel_additional_substances">
                <t t-esc="product.elabel_additional_substances"/>
            </div>
            <div class="ingredient-box" t-if="not product.elabel_additional_substances">-</div>
        </t>
    </template>

    <template id="wine_elabel_page" name="Wine e-Label Page">
        <t t-set="product" t-value="product_tmpl"/>
        <t t-set="elabel_labels" t-value="product._get_elabel_display_labels()"/>
        <html t-att-lang="lang_code and lang_code.replace('_', '-')">
            <head>
                <meta charset="utf-8"/>
                <meta name="viewport" content="width=device-width, initial-scale=1"/>
                <title>
                    <t t-esc="product.display_name"/> - <t t-out="elabel_labels['title']"/>
                </title>
                <style>
                    :root {
                        color-scheme: light;
                        --bg: #f7f5ef;
                        --card: #ffffff;
                        --text: #1f1f1f;
                        --muted: #6b6f76;
                        --line: #d9d9d9;
                        --accent: #7f0f28;
                    }
                    * { box-sizing: border-box; }
                    body {
                        margin: 0;
                        background: radial-gradient(circle at top right, #fff6f6, var(--bg) 48%);
                        color: var(--text);
                        font-family: "Segoe UI", -apple-system, BlinkMacSystemFont, "Helvetica Neue", sans-serif;
                        line-height: 1.45;
                    }
                    .container {
                        max-width: 760px;
                        margin: 32px auto;
                        padding: 0 16px;
                    }
                    .card {
                        background: var(--card);
                        border: 1px solid var(--line);
                        border-radius: 16px;
                        padding: 24px;
                    }
                    h1 {
                        margin: 0 0 6px;
                        font-size: 1.45rem;
                    }
                    h2 {
                        margin: 22px 0 8px;
                        font-size: 1.05rem;
                    }
                    .muted {
                        color: var(--muted);
                        margin: 0;
                    }
                    .ingredient-box {
                        margin-top: 8px;
                        padding: 12px;
                        border: 1px solid var(--line);
                        border-radius: 10px;
                        background: #fcfcfc;
                        white-space: pre-wrap;
                    }
                    table {
                        width: 100%;
                        border-collapse: collapse;
                        margin-top: 8px;
                    }
                    th, td {
                        border: 1px solid var(--line);
                        padding: 10px;
                        text-align: left;
                        font-size: 0.95rem;
                    }
                    th {
                        background: #f4f4f4;
                        width: 56%;
                    }
                    .footer {
                        margin-top: 18px;
                        border-top: 1px solid var(--line);
                        padding-top: 14px;
                        font-size: 0.9rem;
                        color: var(--muted);
                    }
                    .pill {
                        display: inline-block;
                        margin-top: 14px;
                        font-size: 0.8rem;
                        letter-spacing: 0.02em;
                        color: var(--accent);
                        border: 1px solid #e9ccd3;
                        background: #fff4f6;
                        padding: 4px 10px;
                        border-radius: 999px;
                    }
                </style>
            </head>
            <body>
                <main class="container">
                    <section class="card">
                        <h1>
                            <t t-esc="product.display_name"/>
                        </h1>

                        <t t-set="is_pdf" t-value="False"/>
                        <t
                            t-call="odoo_elabel.wine_elabel_information"
                            t-lang="lang_code"
                        />

                        <div class="footer">
                            <t t-out="elabel_labels['mandatory_information_only']"/>
                        </div>
                    </section>
                </main>
            </body>
        </html>
    </template>
</odoo>
